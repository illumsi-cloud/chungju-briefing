# 로컬 실행용 네이버 API 키 템플릿.
# 이 파일을 복사해 .env 로 저장하고 실제 값을 채우세요. (.env 는 git 추적 제외)
#   cp .env.example .env
# GitHub Actions 는 저장소 Secrets 를 쓰므로 .env 가 필요 없습니다.
NAVER_CLIENT_ID=여기에_Client_ID
NAVER_CLIENT_SECRET=여기에_Client_Secret
